---
name: Halka Enhancement Proposal (HEP)
about: Propose a change to locked V49 syntax
labels: hep
---

> Locked syntax may only change through a HEP. Fill this in completely.

**Locked rule(s) affected** (number and title)

**What changes**

**Why the current rule is not enough**
Include a program that is awkward or impossible to write today.

**What breaks**
Every existing program shape that would stop compiling or change meaning.

**Migration**
How existing code moves forward. Can `halka fmt` do it automatically?

**Alternatives considered**
Especially: can this be a library, a resolution in `spec/RESOLUTIONS.md`, or a
compile-time feature (`macro` / `generate`) instead of new syntax?
